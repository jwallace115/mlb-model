NCAAF PARLAY BOARD — WORK ORDER #4, 2026-09-19

READ FIRST: research/ncaaf_board/audit_N09_N11_2026-09-19.md

N09 and N10 are clean and need nothing. N11 reported a successful end-to-end run that was not
one. Three defects, all in item 3's chain:

  - The grader marked three UNPLAYED games graded. For a game that has not kicked off, every
    snapshot satisfies snapshot < commence, so "closing price" resolved to the current line.
    All 12 legs carry clv == exactly 0.0. UPDATE-ONLY then makes them permanently ungradeable.
  - The AI layer never ran. model="claude-sonnet-4-20250514" returns 404 not_found. The error
    string was written into ai_rationale and the run continued and reported success.
  - Layer 2 covered 30 of 146 board teams, because pull_ncaaf_news.py takes its team list from
    the latest tape snapshot while the board is built from the full eligible tape.

ITEM 1 IS TIME-SENSITIVE. Two of the three games kick within 24 hours of this order being
written. Do it first and do not batch it with anything.

Four items. Commit AND push each before the next: git pull --rebase --autostash && git push
Decisions continue the N-series (N12-N15), each in the SAME COMMIT as its item.

--------------------------------------------------------------------------------
ITEM 1 — UN-SEAL THE THREE TICKETS, THEN STOP THE HOLE

The games have not been played, so the decision prices are intact and this is fully
recoverable — but only until someone treats those rows as data.

  1a. In ncaaf/logs/ncaaf_board_tickets_2026.json, for all three tickets: set graded = False,
      and set every leg's close_price and clv back to null. Do NOT touch price, point, book,
      side, market, build_time or commence_time — those are the decision record and they are
      correct. Print the before/after for all 12 legs.

  1b. Add the missing guard to grade_ncaaf_tickets.py: skip any ticket whose commence_time is
      in the future. Grading is undefined before kickoff. State the comparison basis (now, UTC)
      explicitly in the code.

  1c. Add an assertion that halts the grader if every graded leg in a run has clv identically
      0.0. Twelve legs at exactly 0.000 is not a measurement, it is a symptom, and it was
      visible in the output the whole time.

  1d. Write a test that fails without 1b: a ticket with a future commence_time must not be
      graded. Run it against the current implementation and show it goes red first.

DECISION N12: the premature-grading defect, the recovery, and the two guards. Same commit.

--------------------------------------------------------------------------------
ITEM 2 — MAKE THE AI LAYER RUN, OR MAKE IT HALT

  2a. Fix the model id in build_ncaaf_tickets.py:76. Do not guess one. List the models the
      key can actually reach and use one of those; record which, and the date you checked, in
      N13. A model id is a conversation parameter — verify it, do not substitute a plausible one.

  2b. An API failure in the AI layer must HALT the item. It must never be written into
      ai_rationale and it must never let the run report success. A layer that cannot execute
      is a failed run, not a ticket with an unusual rationale field.

  2c. Test the no-pricing-number guard with a CANNED response containing a pricing number —
      a fixture, not a live call. Assert the number is discarded and the discard is logged.
      A guard that has never been exercised is untested no matter how many clean runs pass
      through it. This is the point of the item: N04 asserted the boundary, N11 reported it
      "never triggered", and neither established that it works.

DECISION N13: the verified model id, fail-closed behaviour, and the guard's first real test.
Same commit.

--------------------------------------------------------------------------------
ITEM 3 — LAYERS 1 AND 2 MUST COVER THE SAME GAMES BY CONSTRUCTION

pull_ncaaf_news.py:119-124 reads board_teams from the latest tape snapshot. build_ncaaf_board.py
builds from the full eligible tape. That is why 116 of 146 board teams were never queried.

  3a. Make pull_ncaaf_news.py read its team list from the BOARD ARTIFACT
      (ncaaf/data/board/week=*/ncaaf_board.parquet), not from the tape. One source of truth.

  3b. Assert equality: every team on the board is queried. Report the count both ways and HALT
      on any mismatch. Do not fall back to a partial pull.

  3c. RUNTIME: 146 teams x 1 call x 1s sleep = ~146s. That is fine, but state it before running
      and do not copy the sleep from the 30-team version without recomputing.

  3d. Re-report the zero-article count over the FULL board. A team with no articles is a real
      hole in layer 2 and the board should mark it, not omit it silently.

DECISION N14: board artifact as the single source of truth for layer 2 coverage. Same commit.

--------------------------------------------------------------------------------
ITEM 4 — RE-RUN THE CHAIN, HONESTLY THIS TIME

Re-run board -> news -> tickets. Do NOT re-grade: the two Saturday games kick before any
grading is appropriate, and the Michigan State game is a week out.

REPORT, separating what each command RETURNED from what it MEANS:
  - games on the board, and games dropped by the pre-kick filter
  - board teams queried for news vs board teams total — these must now be equal
  - teams returning zero articles, as a count over the full board
  - AI calls made, AI calls that succeeded, and the discard count, stated separately.
    If the AI layer fails, the run fails. Say so; do not produce tickets without it.
  - every ticket carries reference_only = True
  - every ticket carries graded = False, because nothing has kicked off

Then STOP. Grading happens after the games are played, in a later session, with the item 1
guards in place.

DECISION N15: the first genuine end-to-end run, with counts. Same commit.

--------------------------------------------------------------------------------
CLOSING

Append to logs/agent_sessions.md (git add -f). Timestamp from `date -u`.
State what was NOT done and what remains UNVERIFIED.
Log x-requests-remaining — 8976 as of N01 and unchanged through N11. Zero Odds API credits are
required by this order.

A note on the last three sessions: every item has been reported complete, and three of them
were not. The pattern is that a failure got written into an output field instead of stopping
the run. Prefer halting to producing a deliverable that carries its own error message.
