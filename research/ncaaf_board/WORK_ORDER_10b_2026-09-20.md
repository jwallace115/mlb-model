PARLAY BOARD (NFL + NCAAF) — WORK ORDER #10b, 2026-09-20
CAPTURE FIXES: REPO BLOAT, A READER THAT GOES BLIND, A HEALTH CHECK THAT LIES, MISSING TESTS

Cowork verified order #10 from origin at 2026-09-20 ~03:00Z. The schedule and the pullers are
in and the halt paths exist. Four things are wrong, and the first is time-critical.

WHAT COWORK FOUND (every line reproduced from files on origin)
  A. STORAGE. N29's own table: NCAAF news 10.0 MB/pull gzipped, NFL news 1.1 MB, NFL injuries
     9.2 MB and depth 7.0 MB UNCOMPRESSED, nflverse archive 12 MB/day. At the installed
     cadence that is ~110 MB/DAY into git: news ~44, injuries+depth ~67 (N29 says 480 MB/month
     for these two; (9.156 + 6.970) x ~4.14 pulls/day x 30 = ~2.0 GB), nflverse 12. About
     3.7 GB/month, permanently, in a repo that push_daemon pushes every 30 min and Streamlit
     Cloud clones. Git history cannot be shrunk later without rewriting it.
     Cause, in part, is order #10 itself: it said "raw JSON, do not drop fields". Most of each
     pull is the SAME articles as the pull before, and ESPN's payloads are mostly repeated
     link/headshot/logo structures.
  B. THE TICKET BUILDER GOES BLIND. ncaaf/pipeline/build_ncaaf_tickets.py:307 reads
     news_dir.glob("*.json"). The new puller writes news_<UTC>.json.gz. Next Saturday's card
     would be built from the three 2026-09-19 pulls only, with no error. Order #10's report
     listed this as UNVERIFIED; it is verified broken.
  C. THE HEALTH CHECK LIES, TWICE.
     (1) nfl_props reads "STALE 4,226h" when the newest pull is 2026-09-19T15:11Z (~12 h).
         _newest_props_age compares month=02's tz-NAIVE max (2026-03-28) with month=09's
         tz-AWARE max; the TypeError is swallowed by `except Exception: continue`, so the
         September file is silently skipped. Order #10's report explained the wrong number as
         "correctly identifies props feed dead". It was a swallowed exception.
     (2) Every file-based feed uses st_mtime. In a fresh clone mtime = checkout time: Cowork's
         clone showed ncaaf_news "1.0h old" when the newest pull was ~40 h old. The check was
         specified to run against origin from anywhere; with mtime it cannot. A rebase on the
         VM resets mtimes too.
  D. NO TESTS WERE COMMITTED. `git diff --name-only 0529f52..3f42eb1` contains zero test files.
     Order #10 required: an in-play-filter test that fails without the filter; halt-path tests
     for the ESPN pullers and Kalshi; a health-check fixture test that goes red. The fixture run
     in the report was ad hoc. None of this was listed under NOT DONE.
  Also: the VM now refreshes nfl/data/pbp/depth_charts.parquet daily. That file is TRACKED
  (7.5 MB) — a daily binary commit, and a second writer alongside the Mac (canonical-writer
  rule).

RULES: same as order #10 (GIT_OPTIONAL_LOCKS=0 on every git command; commit AND push each
item; append-only; HALT rather than write an error into data; never print a secret; nothing
under nfl/sim/ and nothing in ~/mlb-model-5i or branch eng/5i; zero Odds API credits in this
order). Decisions N32-N35 in research/ncaaf_board/NCAAF_BOARD_DECISION_v1.md, same commit.
Tests go in shared/pipeline/tests/ and nfl/pipeline/tests/ — never under nfl/sim/.

================================================================================
ITEM 1 — STOP THE BLEEDING FIRST (minutes, before anything else)

On the VM: paste `crontab -l`. Comment out (do not delete) the ESPN news x2, ESPN status x2
and nflverse entries. Leave props, Kalshi, health check and every pre-existing entry alone.
Paste `crontab -l` again.
Then `git status --short data/` on the VM: list every untracked or modified file over 1 MB
with its size. One-off proof outputs from order #10 that are uncommitted AND oversized (raw
.json news, uncompressed injuries/depth) are deleted ONLY if they have not been pushed — list
each one you delete. Anything already on origin stays; say how many MB that is.

DECISION N32: what was paused, what was already pushed, what was removed before it was.

================================================================================
ITEM 2 — STORAGE: SAME INFORMATION, A FRACTION OF THE BYTES

Target: ALL football capture under 300 MB/month in git, MEASURED, not projected from one pull.

  2a. News (both sports): de-duplicate across pulls. Keep a small state file
      data/news_archive/<sport>/season=2026/_seen.json mapping article id -> lastModified (or
      `published` where ESPN gives no lastModified — check the real payload and say which).
      Each pull writes:
        news_<UTC>.json.gz     ONLY articles that are new or whose lastModified changed,
                               complete and unmodified (no field dropped)
        index_<UTC>.json.gz    every (team_id, article_id) present in this pull
      That preserves point-in-time exactly — what was visible at each pull is the index; the
      content of any article as of that pull is its latest version at or before it — while
      storing each article once. The FIRST run after this change writes everything (no state).
      Freshness halt unchanged (newest article within 72 h).
  2b. Injuries and depth charts: gzip (injuries_<UTC>.json.gz, depth_<UTC>.json.gz), and skip
      the write when the payload's content hash equals the previous capture's — but then
      append one line {utc, sha256, "unchanged"} to _pulls.jsonl so a skipped write is still
      evidence the pull happened and what it saw. Hash the payload with ESPN's top-level
      `timestamp` field removed, or it will never match.
  2c. nflverse archive: copy a file to the archive only when its sha256 differs from the last
      archived copy of that file; same _pulls.jsonl line otherwise.
      depth_charts.parquet is tracked and both hosts can now write it. Pick ONE writer and
      say which (recommend the VM, since it is the one on a schedule); state in N33 what the
      Mac does instead (git pull). Do not change nfl/sim/pull_nflverse_inputs.py.
  2d. Kalshi: NFL stays every 30 min. NCAAF: every 30 min from Fri 14:00 UTC through
      Sun 05:30 UTC, every 3 h otherwise — 16% of its 2,472 markets are empty books and the
      slate is Saturday. Measure snapshot sizes and state the monthly total.
  2e. Re-enable the cron entries from item 1. Run each puller TWICE, a few minutes apart, and
      report the second run's bytes — that is the steady-state number. Give the measured
      MB/day and MB/month per feed and in total. If the total is over 300 MB/month, say so and
      say what dominates; do not quietly drop a feed or a field to get under it.

DECISION N33: formats, the de-dup key, the single depth-chart writer, measured MB/month.

================================================================================
ITEM 3 — READER AND HEALTH CHECK

  3a. ncaaf/pipeline/build_ncaaf_tickets.py: read BOTH *.json (legacy, three files) and
      news_*.json.gz; ignore index_* and _seen.json; de-duplicate by article id keeping the
      latest version; print the newest pull's UTC time and article count. HALT if the newest
      pull is more than 24 h older than build time — a card built on stale news must not look
      like a card built on current news. --dry-run must show all of this.
  3b. shared/pipeline/capture_health.py:
      - ages come from the UTC timestamp IN THE FILENAME (or pull_timestamp inside the props
        parquet, or the last line of _pulls.jsonl for hash-skipped feeds) — never st_mtime;
      - parse every timestamp to tz-aware UTC before comparing;
      - remove the bare `except Exception: continue`. An unreadable file is a failure of the
        check: name the file, exit non-zero.
      Run it on the VM AND in a fresh clone of origin and paste both. They must agree to
      within the push delay. nfl_props must read ~hours, not 4,226 h.

DECISION N34: the reader contract and the health check's clock.

================================================================================
ITEM 4 — THE TESTS ORDER #10 OWED, COMMITTED

Each must be shown to FAIL against the code it guards (paste the failure), then pass.
  - props in-play filter: a row with pull_timestamp >= commence_time is dropped; fails with
    the filter removed. Compare parsed datetimes, not strings ("...+00:00" vs "...Z").
  - ESPN news: stale fixture (newest article 100 h old) -> non-zero exit, NO file written.
  - ESPN status: 31-team fixture -> non-zero exit, no file written.
  - Kalshi: non-200 and empty `markets` -> non-zero exit, no file written.
  - news de-dup: second pull with one changed and one new article writes exactly those two;
    the index lists all.
  - health check: (i) one stale feed in a fixture tree -> non-zero exit naming it;
    (ii) REGRESSION for C(1): a tz-naive older partition next to a tz-aware newer one ->
    reports the newer; (iii) REGRESSION for C(2): set every fixture file's mtime to now ->
    ages unchanged.
  - ticket builder reader: a directory holding one legacy .json and one .json.gz returns the
    union, de-duplicated; a newest pull 30 h old halts.
Paste the pytest summary. No network in any test.

DECISION N35: test inventory, and an explicit correction of order #10's report — the 4,226 h
reading was a swallowed exception, not a finding, and the tests were not written.

================================================================================
CLOSING
`crontab -l` from the VM in full. Update research/ncaaf_board/capture_status_2026-09-20.md:
per feed, format, measured MB/month, and the first OBSERVED scheduled firing (the 06:10 /
06:20 / 06:30 / 09:00 / Sun 15:00 UTC slots may have passed by then — check syslog and the
files). Session-log entry (git add -f logs/agent_sessions.md): RETURNED vs MEANS, NOT DONE,
UNVERIFIED.
