NCAAF PARLAY BOARD — WORK ORDER #5, 2026-09-19

READ FIRST: research/ncaaf_board/audit_N12_N15_2026-09-19.md

Order #4 was largely good: the un-sealing held, the future-kickoff guard and all-zero-CLV
assertion are real and tested, the AI layer runs 5/5, and layer 2 now covers 146/146 board
teams. Two things remain, and ITEM 1 MUST LAND BEFORE ANY GRADING RUN.

Three items. Commit AND push each before the next: git pull --rebase --autostash && git push
Decisions continue the N-series (N16-N18), each in the SAME COMMIT as its item.

--------------------------------------------------------------------------------
ITEM 1 — MAKE THE APPEND-ONLY TICKET LOG ACTUALLY APPEND-ONLY

The writer appends correctly (build_ncaaf_tickets.py:198-204, no dedup anywhere). The log
still went from 3 tickets at dd7a3cf6c to 5 at a040034e3 when append would have given 8. Three
rows left through a path that is not in the committed code and nothing noticed.

It was harmless this time — the lost tickets were built while the AI layer was 404ing. It will
not be harmless once graded CLV rows exist, and grading is next.

  1a. Add a guard that runs on every write to ncaaf/logs/ncaaf_board_tickets_2026.json:
      - the ticket count must never decrease
      - no (event_id, build_time) key present on disk may be absent afterwards
      Halt on either. State the before/after counts on every run.

  1b. Write a test that fails without 1a: construct a log with N tickets, write a payload that
      drops one, assert it halts. Run it against the current implementation and show it red
      first.

  1c. Find the deletion path if you can — check whether any script, the un-seal step, or a
      launchd/cron job rewrites this file. If you cannot find it, say so plainly; the guard
      protects the asset either way and that is the point.

DECISION N16: append-only enforced by assertion, not by convention. Same commit.

--------------------------------------------------------------------------------
ITEM 2 — TEST THE NO-PRICING-NUMBER GUARD FOR REAL

test_ai_guard_5d4.py::test_pricing_number_discarded currently asserts isinstance(flags, list),
isinstance(rationale, str) and veto == False. Those are type checks. None of them asserts a
pricing number was discarded. The test passes whether the guard fires or not.

This guard has been reported as handled three times (N04 in prose, N11 "never triggered",
N13 "discards work") and has never been demonstrated.

  2a. Rewrite the test: feed a CANNED response containing a pricing field (a fair probability
      and a fair American price). Assert the returned structure contains NO numeric pricing
      field, and assert the discard was logged.
  2b. Show it red against a version of the handler with the discard removed. A guard test that
      cannot fail is not a test.

This boundary is the only thing keeping the AI layer from becoming an unauditable model inside
a build whose entire claim is that nothing is fitted. Establish it.

DECISION N17: the guard's first real demonstration. Same commit.

--------------------------------------------------------------------------------
ITEM 3 — GRADE, AFTER THE GAMES ARE PLAYED

DO NOT RUN THIS until the games have settled. Georgia @ Arkansas kicks 2026-09-19T16:00Z,
Maine @ Boston College 18:00Z, Ball State @ Liberty 20:00Z, Colorado @ Northwestern 23:30Z.
Nebraska @ Michigan State is 2026-09-26T21:00Z and must remain ungraded this week — the
item 1 guard should let it sit.

Run grade_ncaaf_tickets.py once. Report:
  - rows changed, and which tickets stayed ungraded and why
  - closing price per leg with the snapshot_utc it came from, and the minutes between that
    snapshot and kickoff (it must be negative — before kickoff)
  - CLV per leg and per ticket, broken out by market and by book (Check 5: an aggregate alone
    is not a result)
  - confirm the all-zero-CLV assertion did NOT fire, and say what the spread of CLVs was

Then run it a SECOND time and report rows changed. Expected 0, and this time that result means
something, because the first run graded settled games rather than sealing future ones.

This is the first real CLV data the build has produced. Four tickets is not a sample and proves
nothing — CLV needs roughly 125 observations. Report it as a first data point, not a finding.

DECISION N18: first genuine grading run. Same commit.

--------------------------------------------------------------------------------
NOT IN THIS ORDER — the remaining unbuilt layer

CFB CONTEXT FLAGS. research/ncaaf/cfbd_venues.parquet (840 venues: lat/long, elevation, dome,
grass, timezone) joined to research/ncaaf/cfbd_games_2026.parquet gives travel distance,
timezone crossings, altitude, dome/outdoor, rest days, kickoff slot, neutral site and
FBS-vs-FCS mismatch. Flags the AI layer reads, never weights. Zero credits. Next order.

CLOSING: append to logs/agent_sessions.md (git add -f), timestamp from `date -u`, state what
was NOT done and what remains UNVERIFIED. Zero Odds API credits required; balance was 8976.
