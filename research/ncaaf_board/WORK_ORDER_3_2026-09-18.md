NCAAF PARLAY BOARD — WORK ORDER #3, 2026-09-18

READ FIRST: research/ncaaf_board/audit_N05_N08_2026-09-18.md

A Cowork audit recomputed N06 from the committed parquets. The 21+ result is real and robust
across all eight combinations of window, line key and bin convention. Two other claims are not:

  - The OOS test did NOT use the probe's buckets. The LABELS match; the EDGES do not.
    The probe used right-closed pd.cut bins (|spread|=3 -> "0-3"); test_joint_correlation.py:98
    uses (abs_spread >= lo) & (abs_spread < hi), left-closed (3 -> "3-7"). In football 3/7/14/21
    are the modal spreads, so this reallocates a large mass of games across exactly the
    boundaries under test.
  - "Prediction 3 DID NOT HOLD" is wrong. 7-14 clears |t|>2 in 1 of 8 configurations — the one
    the script used. Under the probe's own bins it is t=1.25. Prediction 3 HELD.

Nobody tuned anything. The convention was re-chosen when the analysis was re-implemented and
the report asserted sameness that was never checked. That is the failure mode to fix.

Three items. Commit AND push each before the next: git pull --rebase --autostash && git push
Decisions continue the N-series (N09-N11), each in the SAME COMMIT as its item.

--------------------------------------------------------------------------------
ITEM 1 — RECORD THE DEFECT, PARAMETERISE THE CONVENTION, CORRECT THE RECORD

EDIT ncaaf/pipeline/test_joint_correlation.py:
  Make the bin convention an explicit parameter (--bins left|right, no default — the caller
  must choose, the same way run_fit.py --out-dir is required). Report BOTH conventions in the
  output table, side by side, for every bucket. A result that depends on which side of a key
  number a game lands is not a result, and the only way to see that is to print both.

  Add an assertion: the bucket edges used must be recorded in the output file itself, so a
  later reader can tell what was actually run without reading the source.

CORRECT research/ncaaf_board/joint_correlation_oos_2025.md IN PLACE:
  - Prediction 3: change DID NOT HOLD -> HELD, with the t=1.25 figure under the probe's bins.
  - Withdraw the "extends further toward the middle" interpretation entirely. Do not soften it,
    do not reword it, delete it and say what replaced it.
  - Add the robustness matrix from §3 of the audit.
  - Leave the original text visible as struck/quoted history. The repo's convention is that
    withdrawn findings stay visible (see the Kalshi settlement-grade withdrawal) so nobody
    re-derives them later.

DECISION N09: the bin-convention defect, what it invalidated, and the requirement that any
bucketed test report both conventions. Same commit.

--------------------------------------------------------------------------------
ITEM 2 — REBUILD THE JOINT TABLE ON THE QUANTITY AVAILABLE AT BET TIME

The current table keys on `spread` = the CLOSING line, which N05 itself established differs
from `spreadOpen` on 85% of rows. The board conditions on a pre-kick line at build time, which
is not the close. Research object != live object: Check 3.

REBUILD joint_outcome_table_v1.parquet -> joint_outcome_table_v2.parquet keyed on spreadOpen.

  Restrict to |spread| >= 21 ONLY, unless 14-21 survives on spreadOpen under BOTH bin
  conventions. It does not in the audit's matrix (t = 0.78 / 0.93 / 1.28 / 1.69), so the
  expected outcome is that the table loses its four 14-21 cells and keeps the four 21+ cells.
  THAT IS THE CORRECT OUTCOME. Do not widen the table to keep its size up.

  Report cell counts. If a 21+ cell falls below n=100 after re-keying, say so — the existing
  21+/OU<48 cell already claims delta=+0.095 on n=100 and that is thin.

  Keep v1 on disk. Do not delete it. It is the record of what was built on the close.

STATE IN THE SPEC, replacing any remaining "N/A by construction" language for the board:
  the joint table is a fitted object; Checks 1b, 2 and 3 now apply to it and must be answered
  honestly — 2022-2024 is discovery-contaminated for this hypothesis, 2025 is the only clean
  OOS window, and the table pools both so its cell values have no clean OOS estimate.

DECISION N10: v2 keyed on spreadOpen, restricted to the bucket that survives. Same commit.

--------------------------------------------------------------------------------
ITEM 3 — RUN A BOARD END TO END. THIS IS THE ONE THAT MATTERS.

Four commits of plumbing exist and have never produced output. ncaaf/data/board/ is empty,
data/news_archive/ does not exist, ncaaf/logs/ncaaf_board_tickets_2026.json is 0 bytes.

Run the full chain for the current week against the existing tape:
  1. build_ncaaf_board.py       — zero credits, reads the tape
  2. pull_ncaaf_news.py         — zero credits, keyless ESPN
  3. build_ncaaf_tickets.py     — Anthropic calls only
  4. grade_ncaaf_tickets.py     — after the games settle

REPORT, separating what each command RETURNED from what it MEANS:
  - how many games the board covered, and how many were dropped by the pre-kick filter
  - how many teams got news, and how many returned zero articles (a team with no articles is
    a silent hole in layer 2 — count it, do not let it pass)
  - how many AI responses contained a numeric pricing field that had to be discarded (the
    N04 boundary). If that count is zero on every game, say whether the guard was actually
    exercised or simply never triggered — an untriggered guard is untested, not proven.
  - the REFERENCE_ONLY flag present on every ticket

RUN THE CLV GRADER NULL CONTROL, which N04 left untested: run it twice, report rows changed
on each pass. Expected N then 0. If the second pass changes any row, the UPDATE-ONLY property
is broken and that is a HALT.

DECISION N11: first end-to-end run, with the counts. Same commit.

--------------------------------------------------------------------------------
CLOSING

Append to logs/agent_sessions.md (git add -f). Timestamp from `date -u`.
State what was NOT done and what remains UNVERIFIED.
Log x-requests-remaining — it was 8976 after N01 and unchanged through N05-N08.
At ~290 credits/day from the three-sport snapshots that is roughly 31 days of runway against
a 3,000 alert threshold. If this order's runs move it, say by how much.
