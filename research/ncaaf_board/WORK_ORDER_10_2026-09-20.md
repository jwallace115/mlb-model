PARLAY BOARD (NFL + NCAAF) — WORK ORDER #10, 2026-09-20
SCHEDULED CAPTURE: PROPS, NEWS, INJURIES, DEPTH CHARTS, KALSHI

THE GOAL, in Jeff's words: get the prop lines running regularly; regularly pull NCAA and NFL
news, injuries and depth charts; collect Kalshi data for NFL and NCAA. Start collecting now.
If the Odds API plan has to go up afterwards, it can.

WHAT IS TRUE TODAY (Cowork, counted from files on origin 2026-09-20 ~01:30Z):
  - Game lines: NFL and NCAAF every 30 min 14:00-05:30 UTC (32/day each), MLB every 15 min.
    Working. Do not touch them.
  - NFL Hard Rock props: 5 pulls all season, EVERY ONE manual or one-off. The three cron
    entries from D84 (Tue 10:00 / Thu 22:00 / Sun 15:00 UTC) have never fired on their own.
  - NCAAF news: 3 pulls, all 2026-09-19, all run by hand. No schedule.
  - NFL news: nothing. NFL injuries/depth/rosters: nfl/sim/pull_nflverse_inputs.py, by hand;
    the feed sat dead for 4.5 days in a game week (CLAUDE.md, KNOWN REDS).
  - Kalshi NFL/NCAAF: nothing captured.

WHERE THIS RUNS: the VM (CLAUDE.md "VM-default rule": root@142.93.242.4, repo /root/mlb-model,
venv /root/mlb-model/venv, push_daemon.sh syncs to GitHub every 30 min). Write the code on the
Mac in ~/mlb-model on main, commit, push, pull on the VM, install cron on the VM.
Exactly ONE host captures each feed (canonical-writer rule). Do the CLAUDE.md decision test
for each new source first: SSH to the VM, make the call, 200 OK -> VM. If a source blocks the
VM's IP, that feed runs on the Mac via launchd and you say so in the decision entry.

NOTE: a separate Claude Code session may be running Phase 5I in the worktree ~/mlb-model-5i on
branch eng/5i. Do not touch that directory, that branch, or anything under nfl/sim/. This
order adds files under shared/pipeline/ and nfl/pipeline/ only, plus cron on the VM.

STANDING RULES
  - 4 items. Commit AND push each before the next. Decisions continue the N-series as
    N28-N31 in research/ncaaf_board/NCAAF_BOARD_DECISION_v1.md (N24-N27 are reserved by
    order #9), same commit as the change.
  - Prefix EVERY git command with GIT_OPTIONAL_LOCKS=0. Push with
    git pull --rebase --autostash && git push. logs/ is gitignored: git add -f.
  - APPEND-ONLY. Every capture writes a NEW timestamped file. Nothing overwrites a prior
    capture. A point-in-time archive is the whole asset: a feed that overwrites itself
    cannot be used for any historical work later (project research rule, check 1b).
  - HALT, do not write an error into a data file. A non-200, an empty payload, a payload
    that fails its freshness check: exit non-zero, log the reason, write NO data file.
    (Order #5's lesson: a 404 was once written into ai_rationale and reported as success.)
  - Secrets: only .env, load_dotenv(path, override=True), log sha256(key)[:8]. Never print a
    key. Kalshi market data needs NO key (verified below) — do not load one for it.
  - Cron lines: absolute interpreter path, absolute log path, no % characters.
  - A diff is not evidence that code runs; an installed cron line is not evidence it fires.
    Evidence is a syslog CRON line plus the file it wrote (D84's method).
  - Measure, do not assume: every endpoint below was checked by Cowork through a summarising
    fetcher, which is good for "does it answer" and bad for exact values. Re-verify each one
    from the VM and paste the real response shape before building on it.

================================================================================
ITEM 1 — NFL HARD ROCK PROPS ON A REAL SCHEDULE

Puller exists: nfl/pipeline/pull_hardrock_props.py (--window-hours, --tag open|mid|close,
--dry-run, HALT below 3,000 credits, 10 markets). Do not change what it captures.

COST MODEL (measured, not estimated): 16 events = 150 credits (D84), 15 events = 141
(2026-09-18). ~9.4 credits per event, +1 for the events list.

  1a. FIRST, find out whether the D84 entries fire. `crontab -l` on the VM, then
      grep CRON /var/log/syslog* for pull_hardrock_props. The Sunday 15:00 UTC slot on
      2026-09-20 is the first scheduled slot since D84. If this order runs after it: did it
      fire, and did rows with snapshot_tag=close and that pull_timestamp land? Paste both.
      If it did NOT fire, find out why BEFORE adding more entries — more lines on a broken
      crontab is five silent failures instead of three.

  1b. Install this schedule (UTC). Keep D84's three entries; add the rest.

        0 10 * * 2      --window-hours 168 --tag open     Tue 6am ET    ~160   (exists)
        0 16 * * 3-6    --window-hours 168 --tag mid      Wed-Sat noon  ~150-160 each, ~620
        0 22 * * 4      --window-hours 12  --tag close    Thu 6pm ET    ~11    (exists)
        0 15 * * 0      --window-hours 12  --tag close    Sun 11am ET   ~140   (exists)
        30 16 * * 0     --window-hours 2   --tag close    Sun 12:30 ET  ~90  (1pm games)
        45 19 * * 0     --window-hours 2   --tag close    Sun 3:45 ET   ~40  (late games)
        50 23 * * 0     --window-hours 2   --tag close    Sun 7:50 ET   ~11  (SNF)
        45 23 * * 1     --window-hours 2   --tag close    Mon 7:45 ET   ~11-21 (MNF)

      TOTAL ~1,090 credits/week (~156/day). The existing line cron burns ~290/day.
      Combined ~446/day = ~13,400/month.

      DST: the VM's cron is UTC. These slots are set for EDT (UTC-4). After 2026-11-01 the
      same UTC slots land one hour EARLIER relative to kickoff; every 2-hour window still
      contains its kickoffs (checked: 1pm ET = 18:00 UTC is inside 16:30+2h), so nothing is
      missed, the last price is just taken ~90 min out instead of ~30. Note it; do not
      add a second set of entries.

      The kickoff-relative slots assume standard windows. Holiday, Saturday, international
      and flexed games will be missed by them; the Wed-Sat mids and Sunday 15:00 still catch
      those games' props, just not their last pre-kick price. Say so in N28; do not build a
      schedule-aware dispatcher in this order.

  1c. CHECK THE PULLER AGAINST IN-PLAY ROWS. With a 2-hour window at 12:30 ET there should be
      no started games in the response, but verify: every row written must have
      pull_timestamp < commence_time. If the puller does not already enforce that, add the
      filter and a test that fails without it (the NCAAF tape's -14.5 @ +970 in-play row is
      the precedent).

  1d. PLAN ARITHMETIC FOR JEFF. Read x-requests-remaining and x-requests-used from a
      --dry-run and report them with the date. Odds API plans (the-odds-api.com, read
      2026-09-20): 20K credits $30/mo, 100K $59/mo, 5M $119/mo. 13,400/month fits the 20K
      plan with ~6,600 spare. State which plan the account is actually on if the headers or
      the account page show it; if not, say it is unknown. Do not upgrade anything.

  PROOF (D84's method): one one-off --dry-run cron entry a few minutes out, show the syslog
  CRON line and the log it wrote, remove it. Then `crontab -l` pasted in full.

DECISION N28: the schedule, the measured cost, the plan arithmetic, what the fixed slots miss.

================================================================================
ITEM 2 — ESPN: NEWS (NFL + NCAAF), NFL INJURIES, NFL DEPTH CHARTS

No key, zero credits. Base: https://site.api.espn.com/apis/site/v2/sports/football/

  SOURCES, and what Cowork saw on 2026-09-20:
    nfl/news?team={id}&limit=20                 answers; articles carry team and athlete ids
    college-football/news?team={id}&limit=20    already used by ncaaf/pipeline/pull_ncaaf_news.py
    nfl/injuries                                answers; per team: athlete id, position,
                                                status, date, type, detail, comment
    nfl/teams/{id}/depthcharts                  answers; formations -> positions -> ranked athletes
    college-football/teams/{id}/injuries        returns {}            (confirms the handoff)
    college-football/teams/{id}/depthcharts     returns no depthchart key
  So: COLLEGE HAS NO FREE STRUCTURED INJURY OR DEPTH-CHART FEED. For NCAAF, news IS the
  injury layer. Do not scrape team sites and do not invent a source; record it in N29.
  CFBD (key already in .env, ncaaf/pipeline/pull_cfbd_season.py) has rosters and player game
  stats but no injuries and no depth charts — out of scope here.

  2a. Generalise, do not duplicate: move the news puller to
      shared/pipeline/pull_espn_news.py --sport nfl|ncaaf, leaving
      ncaaf/pipeline/pull_ncaaf_news.py as a thin wrapper so the board builder keeps working.
      NFL = all 32 teams (build nfl/pipeline/espn_team_map.json from the ESPN teams endpoint;
      assert 32). NCAAF = the board's teams, as today (146 on the last board).
      Output, raw JSON, append-only:
        data/news_archive/nfl/season=2026/news_<UTC>.json
        data/news_archive/ncaaf/season=2026/news_<UTC>.json          (existing path)

  2b. shared/pipeline/pull_espn_nfl_status.py — one run writes BOTH:
        data/injury_archive/nfl/season=2026/injuries_<UTC>.json
        data/depth_archive/nfl/season=2026/depth_<UTC>.json   (all 32 teams in one file)
      Assert 32 teams in each. The league injuries endpoint may or may not return all 32 in
      one response — MEASURE it; if not, loop the 32 team endpoints.

  2c. FRESHNESS CHECKS — these are the halt conditions:
        news: newest article published within 72 h, else HALT (Cowork's fetch of the
              league-wide nfl/news returned a first article dated 2026-08-20; find out
              whether that is the endpoint or the fetcher, and use the per-team form)
        depth: ESPN's payload timestamp — Cowork saw 2026-07-26 on KC. If the depth chart
              itself is stale in-season, SAY SO and keep capturing; the nflverse feed is the
              primary and this is the cross-check
        every file: valid JSON, non-empty, expected team count

  2d. nflverse inputs on a schedule: nfl/sim/pull_nflverse_inputs.py is the feed that died for
      4.5 days. Is it in any crontab? If not, schedule it. It OVERWRITES its parquets (the
      sim reads them); leave that, but ALSO copy each refreshed file to
        data/depth_archive/nfl/season=2026/nflverse_<name>_<UTC>.parquet
      so there is a point-in-time trail. Do not edit the script's behaviour for the sim.

  2e. RUNTIME, calculated: news = (32 + ~146) requests x 0.5 s sleep = ~90 s per run.
      status = 1 + 32 requests x 0.5 s = ~17 s. Measure one run of each and report.

  SCHEDULE (UTC):
        10 0,6,12,18 * * *     pull_espn_news.py --sport nfl
        20 0,6,12,18 * * *     pull_espn_news.py --sport ncaaf
        30 0,6,12,18 * * *     pull_espn_nfl_status.py
        40 16 * * 0            pull_espn_nfl_status.py      Sun 12:40 ET, after inactives
        0 9 * * *              pull_nflverse_inputs.py + archive copy

  STORAGE: the NCAAF news files run 0.2-1.5 MB each. 4/day x 2 sports is ~250 MB/month in
  git. Report the measured sizes. If a month projects past ~300 MB, gzip the JSON
  (news_<UTC>.json.gz) — say which you did. Do not drop fields to save space.

  PROOF: a one-off cron run of each script; syslog line + the file it wrote, with its team
  count and newest-article timestamp. Tests: the halt paths must fail without the guard
  (feed the freshness check a stale fixture; feed the team-count check 31 teams).

DECISION N29: sources, what college does not have, paths, schedule, measured sizes.

================================================================================
ITEM 3 — KALSHI: NFL AND NCAAF GAME MARKETS

Public market data, NO authentication. Base: https://api.elections.kalshi.com/trade-api/v2
Cowork confirmed all six series answer on 2026-09-20:
    KXNFLGAME      e.g. KXNFLGAME-26SEP28PHICHI-PHI   "Philadelphia wins"  bid .47 / ask .48
    KXNFLSPREAD    e.g. KXNFLSPREAD-26SEP24ATLGB-GB8  "GB wins by over 7.5 points?"
    KXNFLTOTAL     e.g. KXNFLTOTAL-26SEP24ATLGB-68    "over 67.5 points scored?"
    KXNCAAFGAME    e.g. KXNCAAFGAME-26OCT03INDRUTG-RUTG   bid .07 / ask .93, volume 0
    KXNCAAFSPREAD, KXNCAAFTOTAL
NFL markets trade; many NCAAF markets are empty books (0 volume, 7/93 quotes). Capture them
anyway — the emptiness is a fact worth having — but record it per row.

  3a. DISCOVER, do not hardcode: GET /series?category=Sports, keep every series whose title
      contains "Pro Football" or "College Football", write the list with ticker/title to
      research/ncaaf_board/kalshi_football_series_2026-09-20.md. Flag any player-prop
      series. Capture the six above plus any per-game prop series found; leave season-long
      futures out.

  3b. shared/pipeline/pull_kalshi_football.py --sport nfl|ncaaf
      GET /markets?series_ticker=...&status=open&limit=1000, follow `cursor` to the end.
      One parquet per run, append-only:
        data/odds_archive/kalshi/nfl/season=2026/snap_<UTC>.parquet
        data/odds_archive/kalshi/ncaaf/season=2026/snap_<UTC>.parquet
      Columns at least: pull_timestamp, series_ticker, event_ticker, ticker, title, subtitle,
      yes_bid, yes_ask, no_bid, no_ask, last_price, volume, volume_24h, open_interest,
      liquidity, open_time, close_time, expected_expiration_time, status — keep whatever the
      API names them; do not rename prices into probabilities here.
      PRICE SCALE: the fetcher showed dollars (0.47); the API has also served cents (47).
      Record which field and scale you stored. Every comparison of two prices states the
      scale of each side (three defects came from mixing scales: D58, D64, D82).
      First check research/execution_edge/kalshi_parse_to_parquet.py for an existing column
      convention and reuse it if it fits.

  3c. IN-PLAY: Kalshi markets stay open during games. Store the kickoff where the API gives
      one; if it does not, store close_time and expected_expiration_time and say plainly
      that pre-game filtering will need the schedule joined on later. Do not guess a kickoff.

  3d. RATE AND RUNTIME, calculated: NFL ~16 games x ~25 markets = ~400 markets = 1-2 pages
      per series x 3 series = ~5 requests. NCAAF ~60 games x ~25 = ~1,500 = ~2 pages x 3
      = ~6 requests. ~11 requests per cycle at 0.25 s apart = a few seconds. Kalshi's basic
      tier allows far more than this. Measure and report pages, rows and seconds.

  SCHEDULE (UTC), aligned with the 30-min line tape so the two are time-matched:
        5,35 0-5,14-23 * * *   pull_kalshi_football.py --sport nfl
        7,37 0-5,14-23 * * *   pull_kalshi_football.py --sport ncaaf
      (5 minutes after the line snapshots so the two jobs do not collide.)

  STORAGE: measure one snapshot of each; 32/day x 2. Report the monthly projection.

  PROOF: one-off cron run of each; syslog line + file + row count + the share of rows with
  zero volume. Halt tests: a non-200 and an empty `markets` list must exit non-zero and
  write no file.

DECISION N30: series list, fields and price scale, what is and is not pre-game-filterable.

================================================================================
ITEM 4 — A HEALTH CHECK THAT CANNOT LIE, AND THE STATUS DOC

Every silent failure in this repo had the same shape: a schedule that looked installed and
wrote nothing (D84's cron, the 0-byte macOS logs, the dead depth feed).

  4a. shared/pipeline/capture_health.py — for each feed below, find the newest file (or newest
      pull_timestamp inside the props parquet), print its age, and compare with a max age:
          nfl lines 45 min*   ncaaf lines 45 min*   kalshi nfl 45 min*   kalshi ncaaf 45 min*
          nfl props 26 h (Wed-Sun) / 7 d otherwise   nfl news 7 h   ncaaf news 7 h
          nfl injuries+depth (ESPN) 7 h   nflverse inputs 26 h
          (* inside the 14:00-05:30 UTC capture window; outside it, 9 h)
      Exit non-zero if ANY feed is stale, naming it. It reads files only — no network — so
      Cowork can run it against origin from anywhere.
      TEST IT CAN FAIL: point it at a fixture directory with one stale feed and assert the
      non-zero exit and the feed's name. A health check that cannot go red is not one.

  4b. Schedule it hourly on the VM, appending to /root/mlb-model/logs/capture_health.log.
      Do not build alerting in this order.

  4c. Write research/ncaaf_board/capture_status_2026-09-20.md: every feed, its source, its
      script, its cron line, its output path, its measured size and cost, and the FIRST
      scheduled (not one-off) firing you were able to observe for each — or "not yet
      observed" where the slot has not arrived. Do not mark a feed as running on the
      strength of an installed cron line.

DECISION N31: the health check's thresholds and the observed-firing table.

================================================================================
CLOSING

Paste `crontab -l` from the VM in full. Append a session-log entry to logs/agent_sessions.md
(timestamp from `date -u`) separating what each command RETURNED from what it MEANS, and
listing what was NOT done and what remains UNVERIFIED.

WHAT THIS ORDER DOES NOT DO
  - NCAAF player props or Hard Rock NCAAF lines: that is order #9 (Hard Rock has been absent
    from the Odds API's NCAAF feed in every one of 673+ snapshots).
  - The week folder (order #9 item 3). These archives are what it will read from.
  - nfl/sim/run_week.py still reads the newest line snapshot with no kickoff filter. Real,
    money-adjacent, and NOT touched here because Phase 5I has nfl/sim/ open on a branch.
  - No modelling, no edge claim. This is a tape.
