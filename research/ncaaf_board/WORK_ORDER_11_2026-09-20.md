NCAAF PARLAY BOARD — WORK ORDER #11, 2026-09-20
REPAIR WHAT AUDIT #4 FOUND: CONTRACT IDENTITY, THE GRADER, THE NEWS CONNECTION, FEED HEALTH

Read first: research/cross_ai/chatgpt_audit4_adjudication_parlay_board_2026-09-20.md (every
finding below was reproduced by Cowork at 9eb533f, with the numbers to reproduce it), then
CLAUDE.md WORK ORDERS.

WHY: nothing this board has logged so far is evidence of anything. 17 of 30 legs in the last
build are contracts no book offered; the selector received news for ONE team of 146; the grader
raises KeyError on the committed log; CLV ignores the line. Each was invisible because the tests
exercise replicas or fixtures, not the production path on production data.

RULES
  - 4 items, commit AND push each before the next. Decisions N37-N40 in
    research/ncaaf_board/NCAAF_BOARD_DECISION_v1.md, same commit as the change.
  - GIT_OPTIONAL_LOCKS=0 on every git command; push with git pull --rebase --autostash && git
    push; logs/ is gitignored (git add -f). Nothing under nfl/sim/. Nothing under bets/ (private,
    gitignored — do not read it, do not commit it). Zero Odds API credits.
  - Append-only logs stay append-only. Existing ticket entries are NOT edited or deleted; they
    get one new field, `pre_repair: true`, and are excluded from every grade and every count.
  - HALT rather than write a wrong value. "Unavailable" is a valid stored result; a guess is not.
  - EVERY TEST IN THIS ORDER IMPORTS THE PRODUCTION FUNCTION AND RUNS IT ON A FIXTURE CUT FROM
    REAL DATA (a slice of the committed tape / archive / ticket log), and is shown to FAIL on the
    code as it stands at 9eb533f before the fix. No replicas. No hand-made schemas. If a function
    is buried in main(), extract it first. Tests in ncaaf/pipeline/tests/ and
    shared/pipeline/tests/. `pytest shared/pipeline/tests nfl/pipeline/tests ncaaf/pipeline/tests`
    must collect and pass in ONE command when you are done (today it errors: two packages named
    `tests`).
  - Pre-register: before each fix, write the number you expect the reproduction to show, run
    it, and say whether it matched the adjudication's number.

================================================================================
ITEM 1 — A LEG IS ONE ROW OF ONE BOOK

build_ncaaf_tickets.py:226-228 writes point = consensus_point, price = best_price, book =
best_book — three fields from up to three different rows. build_ncaaf_board.py's else-branch
(Under, h2h) pairs the median point with the FIRST book's price. The "No-vig" comment in the
board computes a median of one side's raw implied probabilities — it is not de-vigged.

  1a. REPRODUCE: latest AI build (2026-09-19T11), spread/total legs whose (book, point, price)
      is not that book's latest pre-build row. Cowork: 17 of 30. Paste the table.
  1b. A board row offers, per side, a list of REAL quotes: (book, point, price, snapshot_utc,
      book_last_update) — each one row of the tape. `consensus_point` stays as a display field
      and may be shown to the AI; it is never written into a leg.
  1c. A leg is chosen from that list by a written rule: best point for the side (spread: most
      positive; Over: lowest; Under: HIGHEST — today Under gets the consensus), ties broken by
      best price, then by book name. The leg stores that row verbatim plus the complement
      row FROM THE SAME BOOK AND SNAPSHOT (needed for de-vigging at entry).
  1d. Assertion at write time: every leg's (book, market, outcome_name, point, price,
      snapshot_utc) exists in the tape. Raise otherwise.
  1e. Add `pre_repair: true` to the 31 existing entries (field added, nothing else touched;
      the N16 append-only guard must still pass).

DECISION N37.

================================================================================
ITEM 2 — A GRADER THAT RUNS, AND MEASURES THE RIGHT THING

  2a. REPRODUCE: grade_tickets(2026) on a temp copy of the committed log -> KeyError 'event_id'
      (card entries carry event identity on their legs). Paste it.
  2b. Grade LEGS, wherever they live (event tickets and cards). Skip `pre_repair` entries.
  2c. CLOSE = the last pre-kick row for the leg's book/market/outcome with
      commence_time - snapshot_utc <= 30 minutes, AND its same-snapshot complement. Older than
      30 min: store it as `last_observed_*` with its age, and leave CLV fields null. No fallback
      to another book. A leg with no close stays `graded: false` with `grade_status:
      "close_unavailable"`. (Today: any age is accepted and graded=True is unconditional.)
  2d. CLV, two separate quantities — never blended:
        point_clv   spread (same team): entry_point - close_point
                    Over:               close_point - entry_point
                    Under:              entry_point - close_point
                    (-24.5 closing -27.5 = +3.0)
        prob_clv    ONLY if the close quotes the leg's ORIGINAL point at that book:
                    q_c = close price of the original contract, de-vigged with its own
                    same-snapshot complement; q_0 = entry price de-vigged with the ENTRY
                    complement stored in item 1c.  Store q_c - q_0 and C = d_0 * q_c - 1.
                    Otherwise prob_clv = null with reason "line_moved_no_alt_quote".
                    Never substitute the new main line.
      Every stored price says which scale it is on (american / decimal / no-vig prob).
  2e. OUTCOMES from research/ncaaf/cfbd_games_2026.parquet (homePoints/awayPoints, `completed`;
      refresh it with ncaaf/pipeline/pull_cfbd_season.py first and say what that costs — CFBD,
      not the Odds API). win / loss / push per leg; a game not `completed` stays pending.
      Match tape events to CFBD games on teams + date and REPORT the match rate; an unmatched
      leg is `outcome_unavailable`, not a loss.
  2f. The N12 all-zero-CLV assertion stays.

DECISION N38: the CLV definitions verbatim, the 30-minute rule, the outcome join's match rate.

================================================================================
ITEM 3 — THE NEWS HAS TO REACH THE SELECTOR, AS IT WAS KNOWN AT BUILD TIME

  3a. REPRODUCE with the production reader on the committed archive: 14,526 loaded -> 1,381
      retained -> 1 with `team_name` (a 2025-12-01 Wyoming article). Paste it.
  3b. Extract the reader from main() into load_news(news_dir, build_time). One team key:
      accept `_team_name` and legacy `team_name`. Legacy articles have no id: key them on
      sha1(team + published + headline). De-dup keeps the latest version WITH PULL TIME <=
      build_time (`_pull_time` / legacy `pull_time`; for the new format also honour the
      per-pull index) — an article version pulled after the build must not enter it.
  3c. Per game: that game's two teams' articles only, newest `published` first, capped at N
      (state N), plus a recency window (state it). `news_articles[:10]` unsorted goes.
  3d. COVERAGE ASSERTION at build time: print teams-with->=1-article / board teams; HALT below a
      threshold you state and justify (the old layer-2 check was 146 of 146).
  3e. Every ticket stores an input manifest: tape snapshot ids used, article ids shown, model
      id, prompt sha256, raw response. Selected AND abstained games are logged.
  3f. pull_espn_news.py: record per-team failures in the index file; exit non-zero and write
      NOTHING if more than 5% of teams failed (state the measured normal failure rate first).

DECISION N39: the reader contract, the cutoff rule, coverage on the next real build.

================================================================================
ITEM 4 — FEED HEALTH THAT CANNOT BORROW ANOTHER FEED'S PULSE

  4a. REPRODUCE: data/depth_archive/nfl/season=2026/_pulls.jsonl is written by
      pull_espn_nfl_status.py (depth), run_nflverse_with_archive.sh AND
      archive_nflverse_depth_delta.py; capture_health._newest_pulls_age reads the last line
      whoever wrote it, for both `nfl_depth` and `nflverse_inputs`. Show a fresh ESPN line
      making a stale nflverse feed read fresh, in a fixture tree.
  4b. Every _pulls.jsonl line carries `feed` (espn_depth | espn_injuries | nflverse_depth |
      nflverse_injuries | nflverse_rosters). Health filters by feed. Existing lines without
      `feed`: infer from the `file` key where present, otherwise ignore them and say how many.
  4c. COMPLETENESS, not just arrival: each check also asserts the newest artifact's row/team
      count (32 NFL teams; news index team count vs the requested list; Kalshi and line
      snapshots non-empty with >= 1 event). State each threshold.
  4d. One command runs all three test folders (see RULES).

DECISION N40.

================================================================================
CLOSING
Paste the one-command pytest summary. Append a session-log entry (RETURNED vs MEANS, NOT DONE,
UNVERIFIED). Update research/ncaaf_board/capture_status_2026-09-20.md and the claim in
hardrock_sgp_adjustment_2026-09-19.md section 3-4: the "0.746 fair value / 6.3% overcharge" is
WITHDRAWN (the table measures HOME cover on closing totals — build_joint_table.py:50 — and the
interval spans 0.70-0.80). What stands is what Hard Rock charges, measured from real slips.

NOT IN THIS ORDER
  - Rebuilding the joint table on favourite-cover with an untouched validation year.
  - The shadow comparison of AI selection against written baselines (needs items 1-3 first).
  - The 30-quote same-game pricing experiment and the totals-vs-consensus check: both need
    Hard Rock NCAAF quotes, which the Odds API has never served (order #9).
