# Phase 5J-2 work order — finish 5J, then find why the board differs between machines

Date: 2026-09-21 (UTC). Repo: jwallace115/mlb-model. Scope: `nfl/sim/`, `research/nfl_sim/` only.
Read first: `research/nfl_sim/phase5j_verification_2026-09-21.md` (Cowork's verification of 5J — every
item below comes from it), D104-D107 on `eng/5j`, and the AMENDED `research/nfl_sim/workorder_5J_2026-09-20.md`
on main (your 5J session ran the pre-amendment text).

## HARD RULES
1. Continue on branch `eng/5j`, worktree `~/mlb-model-5j`. First: `GIT_OPTIONAL_LOCKS=0 git fetch origin &&
   GIT_OPTIONAL_LOCKS=0 git merge origin/main` (brings the amended order, the post-kick line tape and the
   later props pulls). `main` is not touched. Cowork verifies and merges.
2. `engine.py`, `anchor.py`, `params_v1.json`, `nfl/data/sim/tables/` are NOT edited in items 1-3:
   `engine_fingerprint()` must print `7f3d96900218c014` after each. Item 4 is DIAGNOSIS ONLY — it changes no
   engine file either; if the cause is in the engine, you REPORT it and stop.
3. No existing test/tolerance/threshold edited. New tests go in the 5J test files or a new
   `nfl/sim/tests/test_board_5j2.py`. Every new test is shown FAILING on the pre-change code.
4. 4 items; commit AND push each before the next; each writes its `### DNN` entry (D108+; grep first) in
   the same commit. Every git command prefixed `GIT_OPTIONAL_LOCKS=0`. Zero API credits.
5. Never reduce N, subsample or skip a run. A diff is not evidence — paste execution output.

## Item 1 — a book-quoted line is never missing (finish 5J item 3)
- Bug: a Hard Rock rush-attempts line that EQUALS a rung (4.5/9.5/14.5/19.5) is skipped by the new loop and
  can then be dropped by the rung loop's 0.05-0.95 filter (Tyler Allgeier 9.5 on 2026-09-20). Fix: a line
  the book quotes is always priced, rung or not; the 0.05-0.95 skip applies only to rungs the book does NOT quote.
- Write the test 5J item 3 required and did not get: synthetic props with an RB line of 13.5 AND one of
  9.5 whose sim_p is > 0.95 -> both legs exist with `sim_p == mean(carries >= k)`. FAILS on `9b1e09b`.
- `run_board_coverage.py` takes `--candidates <file>` (default newest) — per the amended order.
- PRE-REGISTERED: `run_week.py --week 2 --as-of 2026-09-20T15:30:00Z` (after item 2's props cap) measured
  against `nfl_prop_candidates_20260920T1614Z.parquet`: RB rush-attempt rows priced at the book's line =
  34 of 36 exactly (Cowork measured 33; the two remaining misses are Jonathon Brooks and TreVeyon Henderson,
  not in the sim universe). Receptions unchanged at 128 of 142. NULL CONTROL: every leg on the pre-change
  board has the same `sim_p` to 4 dp (same machine, same command). Write outputs to
  `research/nfl_sim/phase5j2_item1_board/`; do not commit `nfl/data/sim/outputs/week=2026_02/`.

## Item 2 — `--as-of` is complete and its record is written
- `--as-of` also caps props: `load_props` reads only rows with `pull_timestamp <= as_of` (D69 precedence
  unchanged inside that). Without it a Week 2 re-run now prices Sunday-night pulls.
- `line_snapshot_utc` and `line_book` are WRITTEN: a column in `anchoring_log.parquet` and a line per game
  in the board header. Today they exist only in a dict (grep finds no writer).
- The game set is the union over the snapshots that may be read, not only the newest, so a game that has
  left the feed appears as SKIPPED with a reason instead of vanishing.
- PRE-REGISTERED P3 (now testable — Cowork ran it and it held): `--as-of 2026-09-20T18:30:00Z` -> 8 kicked
  games get their 17:00:07Z pre-kick line (PIT@NE +5.0 / 41.0) where the old function returns in-play lines
  (PIT@NE +13.5 / 36.5). Reproduce it and paste the table. Add a real-tape fixture test for one such game.
  Record in the D-entry that the feed's `commence_time` drifts to the actual kick (17:00:00 -> 17:02:22Z).

## Item 3 — item 1's kickoff clock, and no silent failure
- nflverse `gametime` is US Eastern. Build schedule kickoffs as America/New_York -> UTC (today 20:15 ET is
  read as 20:15Z, 4 h early). PBP `game_date` weeks unchanged (midnight UTC, conservative by design — say so).
- Remove `except Exception: pass`: if the schedule cannot be loaded, HALT with a message — unless every
  week being built has a PBP kickoff. A failed download must not silently produce a week with no layer-3 QB.
- Tests run OFFLINE: commit a small 2026 schedule fixture; no network call inside a test.
- PRE-REGISTERED: zero `is_starting_qb` rows change for 2026 weeks 1-3; 2021-24 block bit-identical
  (paste both hashes). Test: a rank-1 QB row dated 2026-09-24T22:00Z (6pm ET, before the 8:15pm ET Week 3
  opener) IS accepted for week 3 — it is rejected by the current code. Show it failing on `15ec21c`.

## Item 4 — DIAGNOSE (do not fix): the board differs between machines
Measured by Cowork: main's code, identical ratings/rosters/lines/props, anchoring offsets equal to 3
decimals in 15/15 games — yet 621 of 1,237 legs differ between the Mac's 15:50Z board and a Linux re-run,
up to 0.60 (Jeanty O14.5 rush att 0.756 vs 0.275). Two Linux runs agree exactly.
1. Print `python --version`, numpy, pandas, pyarrow versions on the Mac. (Linux: 3.11, check numpy/pandas
   from Cowork's note if given; otherwise report the Mac's.)
2. Single game, `LV@LAC`, `run_anchored_chunked('LAC','LV',2026,2,7.0,43.5)`: print per-player mean
   carries/targets for LV and the ORDER of `renormed` inside `_build_player_context` (player_id list) for
   both teams. Cowork's Linux values: Jeanty 12.26 mean carries, Mike Washington 8.84, Hampton 25.03;
   unanchored `simulate_game(seed=12345, n_sims=1500)`: Jeanty 21.43, Washington 4.77.
3. Name every place in the player path where ORDER or a LIBRARY default can change a result: unstable
   `sort_values` on near-tied shares (engine ~line 473 sorts by `target_share`, default quicksort),
   iteration over `set(...)`, `nlargest` ties, merge order, RNG draws consumed in player order, numpy
   Generator stream differences between versions. For each: is it reachable, and does forcing it
   (e.g. `kind="stable"` + a `player_id` tiebreak, in a SCRATCH copy) change the LV numbers?
4. Separately report, as a measurement: why anchored Jeanty is 12 carries when unanchored is 21 (offsets
   dh=+0.73 / da=-0.94) — which engine rule moves the RB1/RB2 split with game script?
PRE-REGISTERED (Cowork's guess, may be wrong — say so plainly if it is): the cause is an ordering
dependence, and forcing a total order (share, then player_id, stable) makes the Mac reproduce Linux to
within 0.02 on >= 99% of legs. NULL CONTROL: team-level anchoring results do not move.
Deliver `research/nfl_sim/phase5j2_reproducibility.md` + the committed diagnostic script. NO engine edit.

## What this order does NOT do
- It does not merge. It does not re-fit. It does not start 5K. It does not "fix" reproducibility — a fix
  to `engine.py` changes the fingerprint and needs its own order with a re-fit decision.
- It does not reconcile D99-D103's punts "actual 8.73" with the generator's 7.90: Cowork records that
  correction in the decision doc on main.

## Closing
Append to `logs/agent_sessions.md` (`git add -f`), timestamp from `date -u`: what each command RETURNED vs
what it MEANS; NOT DONE; UNVERIFIED — including any prediction you could not test and why, stated as a
fact you checked, not a guess (the 5J report's "line capture stops before kick" was a guess, and wrong).
